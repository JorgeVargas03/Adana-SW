import { Box } from "@mui/material";
import InventoriesTab from "../components/InventoriesTab.jsx";

const Inventories = () => {
    const current = "INVENTORIES";
    return (
        <Box>
            <InventoriesTab />
        </Box>
    );
};

export default Inventories;
